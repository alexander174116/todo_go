# 📝 To-Do CLI Application (Go)

Консольное приложение для управления списком задач. Поддерживает добавление, вывод, выполнение, удаление, импорт и экспорт задач в форматах **JSON** и **CSV**.

---

## 📌 Требования

- **Go 1.16+**
- Терминал / командная строка
- Права на чтение/запись файлов

---

## ⚙️ Запуск

### 🛠️ 1. Сборка приложения
```
go build -o todo ./cmd/todo
```
После этого появится исполняемый файл **todo**.

### ▶️ 2. Запуск
Пример:
```
./todo add --desc="test task"
```

---

## ⚙️ Функциональность

### ➕ 1. Добавить задачу

```
todo add --desc="Описание задачи"
```
Задаче автоматически присваивается уникальный ID.

---

### 📋 2. Вывести список задач

```
todo list --filter=all
todo list --filter=done
todo list --filter=pending
```

Фильтры:

- **all** — все задачи  
- **done** — выполненные  
- **pending** — невыполненные  

---

### ✔️ 3. Отметить задачу выполненной

```
todo complete --id=1234
```

---

### 🗑️ 4. Удалить задачу

```
todo delete --id=1234
```

---

### 📤 5. Экспорт задач

```
todo export --format=json --out=backup.json
todo export --format=csv --out=backup.csv
```

Поддерживаемые форматы: **json**, **csv**

---

### 📥 6. Импорт задач

Заменяет текущий список задач содержимым файла:

```
todo load --file=tasks.json
todo load --file=tasks.csv
```
Поддерживаемые форматы: **json**, **csv**

---

## 📂 Структура проекта

```
todo-app/
│
├── cmd/
│   └── todo/
│       └── main.go
│
├── internal/
│   └── todo/
│       ├── task.go
│       └── manager.go
│   
├── storage/
│   ├── json_storage.go
│   └── csv_storage.go
│
├── tasks.json
└── README.md
```

---

## 🚀 Примеры использования

```
./todo add --desc="Изучить тесты в Go"
./todo add --desc="Попробовать CSV экспорт"
./todo list --filter=pending
./todo complete --id=1
./todo delete --id=2
./todo export --format=json --out=mybackup.json
./todo load --file=mybackup.json
```
